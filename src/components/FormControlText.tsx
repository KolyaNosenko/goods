import styled from "styled-components";
// TODO add props
const FormControlText = styled.span`
  display: block;
  font-size: 12px;
  color: var(--danger);
  margin-top: 3px;
  margin-left: 3px;
`;

export default FormControlText;
